[package]
name = "autotiling"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
swayipc = "3.0"

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
strip = true
